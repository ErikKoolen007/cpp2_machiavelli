#include "ClientInputHandler.h"

void ClientInputHandler::handleInput(ClientInfo& client_info, const std::string& command)
{
	/*auto &client = client_info.get_socket();
	auto &player = client_info.get_player();
	client << player.get_name() << ", id: " << player.id() << ", you wrote: '" << command << "', but I'll ignore that for now.\r\n" << "machiavelli> ";*/
}
